package Expo.module.impl.visual;

import Expo.module.Category;

import Expo.module.Module;











public class ViewClip extends Module {
   private static final long a = 128723754072535L;

   public ViewClip(long var1) {
      super(((a ^ (var1)) ^ 104826091561591L));
      // add code
      this.declare("ViewClip", Category.Visual, "Remove the camera blocking by blocks in 3rd person view");
      var1 = a ^ var1;
   }}
